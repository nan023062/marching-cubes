# DualContouring Contract

当前无任何公开 API（实现为空骨架）。

待实现后更新本文件，预期公开接口参考：
- `DualContouring(int x, int y, int z)` — 构造器
- `SetPointISO(int x, int y, int z, float iso)` — iso 场输入
- `Rebuild()` — 等值面重建（QEF 最小化）
- `Mesh mesh` — 网格输出
